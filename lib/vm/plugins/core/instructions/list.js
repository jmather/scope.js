if (typeof define !== 'function') { var define = require('amdefine')(module); }

define(function(requre) {
    /**
     *
     * @exports ListInstructions
     */
    var instructions = {};
    
    return instructions;
});